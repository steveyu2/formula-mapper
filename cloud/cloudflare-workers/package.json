{
  "name": "formula-mapper-sync-worker",
  "version": "1.0.0",
  "description": "Cloudflare Workers backend for Formula Mapper cloud sync",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.20240605.0"
  }
}

name = "formula-mapper-sync"
main = "index.ts"
compatibility_date = "2024-01-01"

# D1 数据库绑定
[[d1_databases]]
binding = "DB"
database_name = "formula-mapper-db"
database_id = "34d425e2-294f-47a4-a0bf-4e4656673a0f"

# 可选：环境变量
# 注意：密码应该通过 wrangler secret put 设置，而不是写在这里
[vars]
# API_KEY = "your-secret-api-key"

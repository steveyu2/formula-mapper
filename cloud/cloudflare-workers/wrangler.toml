name = "formula-mapper-sync"
main = "index.ts"
compatibility_date = "2024-01-01"

# KV Namespace 绑定
[[kv_namespaces]]
binding = "FORMULA_DATA"
id = "9b3e867f8ed64427b03d4a48cf058d67"

# 可选：环境变量
# 注意：密码应该通过 wrangler secret put 设置，而不是写在这里
[vars]
# API_KEY = "your-secret-api-key"

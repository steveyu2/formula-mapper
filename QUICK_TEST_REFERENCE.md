# 🎯 快速测试参考卡

## ⚡ 核心功能快速验证清单

### ✨ 5 分钟快速冒烟测试

```
□ 1. 创建公式
  输入：a + b
  中文：变量a + 变量b
  验证：公式出现在列表 ✓

□ 2. 渲染验证
  验证：变量显示不同颜色 ✓
  验证：公式渲染正确 ✓

□ 3. 函数测试
  输入：max(a, b)
  验证：函数名紫色显示 ✓

□ 4. 分组管理
  创建分组 > 移动公式 > 验证 ✓

□ 5. 数据导出
  导出 JSON > 验证文件 ✓
```

---

## 🎨 快速功能矩阵

### 公式操作
| 操作 | 快速验证 | 状态 |
|-----|--------|------|
| 创建 | 新建 → 保存 → 列表 | ✅ |
| 编辑 | 修改 → 更新 → 验证 | ✅ |
| 删除 | 删除 → 确认 → 消失 | ✅ |
| 复制 | 复制 → 新 ID → 列表 | ✅ |

### 函数支持
| 函数 | 验证点 | 快捷验证 |
|-----|--------|---------|
| max | 紫色标签+多参数 | max(a,b) |
| min | 嵌套支持 | min(max(a,b),c) |
| sum | 参数解析 | sum(a,b,c) |
| sqrt | LaTeX显示 | sqrt(x) → √x |
| pow | 指数显示 | pow(x,2) → x² |
| abs | 绝对值符号 | abs(a-b) → \|a-b\| |

### 变量映射
| 场景 | 验证 | 通过/失败 |
|------|------|---------|
| 自动生成 | a→价格, b→数量 | ⬜ |
| 手动编辑 | 修改立即生效 | ⬜ |
| 中英切换 | 显示对应语言 | ⬜ |
| 数字不匹配 | 显示警告 | ⬜ |

---

## 📝 最常用测试用例

### TC-001: 创建基础公式 (必须测试 ✓✓✓)
```
步骤：新建 → 输入 → 保存
验证：列表中出现 + LocalStorage 保存
时间：2-3 分钟
```

### TC-002: 函数渲染 (必须测试 ✓✓✓)
```
步骤：创建 max(a,b) → 查看渲染
验证：函数名紫色 + 参数彩色 + LaTeX 正确
时间：2 分钟
```

### TC-029: JSON 导出 (必须测试 ✓✓)
```
步骤：数据管理 → 导出 → 验证文件
验证：文件下载 + 结构完整 + 数据正确
时间：3 分钟
```

### TC-030: JSON 导入 (必须测试 ✓✓)
```
步骤：清空 → 导入 → 验证数据
验证：数据完整恢复 + 公式正确 + 分组完整
时间：3 分钟
```

### TC-043: 验证错误处理 (重要测试 ✓)
```
步骤：创建空公式 → 观察提示
验证：显示错误 + 阻止保存
时间：1 分钟
```

---

## 🐛 常见问题快速排查

| 问题 | 排查步骤 | 解决方案 |
|-----|--------|--------|
| 函数不渲染 | 检查语法 → 重新创建 | 确保用 max(a,b) 格式 |
| 变量映射错误 | 检查变量顺序 | 中英变量需按顺序对应 |
| 导入失败 | 验证 JSON 格式 | 使用导出的文件重新导入 |
| 数据丢失 | 检查 LocalStorage | F12 → Application → LocalStorage |
| 渲染卡顿 | 减少公式数量 | 测试小数据集先 |

---

## 🔑 关键验证点

### 必须验证的功能
- [ ] 公式创建和保存
- [ ] 函数识别和渲染
- [ ] 变量映射正确性
- [ ] 分组管理完整性
- [ ] 导入导出功能
- [ ] 数据持久化
- [ ] 浏览器兼容性

### 不能遗漏的边界测试
- [ ] 空输入处理
- [ ] 特殊字符支持
- [ ] 长公式处理
- [ ] 并发操作
- [ ] 内存泄漏

---

## 📊 测试签核表

### 每日测试总结
```
日期：____年__月__日
测试人：_____________
环境：Chrome / Firefox / Safari / Edge
当日测试数：___
通过数：___
失败数：___
阻塞数：___

发现的 Bug：
□ 高严重级 ___
□ 中严重级 ___
□ 低严重级 ___

签名：____________  时间：_______
```

---

## ⚙️ 实用命令速查

### 测试相关
```bash
npm test                    # 运行所有测试
npm run test:watch        # 监视模式
npm run test:coverage     # 覆盖率报告
npm test -- --verbose    # 详细输出
```

### 项目运行
```bash
npm run dev               # 开发模式
npm run build            # 构建项目
npm start                # 生产模式
```

### 数据相关
```bash
# 查看 LocalStorage（F12 打开）
# Application → LocalStorage → [域名]
# 键名：formulaMapper
```

---

## 📱 函数语法速查

```
max(a, b, c)           # 最大值
min(x, y, z)           # 最小值
sum(a, b, c)           # 求和
abs(x)                 # 绝对值
sqrt(x)                # 平方根
pow(x, n)              # 幂函数

# 嵌套示例
max(min(a, b), c)      # 嵌套函数
max(a + b, c * d)      # 函数+表达式
(a + b) * max(c, d)    # 表达式+函数
```

---

## 🎯 按优先级的测试顺序

**优先级 1（必须测试，每次都测）**
1. TC-001: 创建公式
2. TC-002: 函数渲染
3. TC-029: 导出 JSON
4. TC-030: 导入 JSON

**优先级 2（重要功能）**
5. TC-004: 编辑公式
6. TC-005: 删除公式
7. TC-007: 创建分组
8. TC-009: 移动公式

**优先级 3（完整性）**
9. TC-013-016: 变量映射
10. TC-021-027: 函数测试
11. TC-034-037: 搜索筛选

**优先级 4（边界/兼容）**
12. TC-041-048: 边界测试

---

## 📋 测试报告模板

```
测试报告 - [日期]

总体结果：PASS / FAIL

通过率：___%

关键功能状态：
✅ 公式管理：OK
✅ 分组管理：OK
✅ 函数支持：OK
⚠️ 云端同步：需确认
❌ 导入导出：失败

发现的问题：
1. [问题描述] - 严重级别：高/中/低
2. [问题描述] - 严重级别：高/中/低

建议：
- [建议 1]
- [建议 2]

签名：________  日期：_______
```

---

## 🔄 回归测试清单

每次新版本发布前：
- [ ] 运行自动化测试（需 100% 通过）
- [ ] 执行优先级 1 的 4 个测试用例
- [ ] 验证所有函数正常工作
- [ ] 测试导入导出功能
- [ ] 检查浏览器兼容性
- [ ] 验证数据持久化

---

## 📞 快速联系

- 技术支持：开发团队
- 功能咨询：产品经理
- Bug 反馈：GitHub Issues

---

**版本**: 1.0  
**最后更新**: 2024年  
**打印友好**: 是 🖨️

